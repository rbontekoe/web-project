package model.test

import model.domain.RoomType

object Test {;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(73); val res$0 = 
 RoomType;System.out.println("""res0: model.domain.RoomType.type = """ + $show(res$0))}
}
